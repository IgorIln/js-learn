// Alert 

alert("Hi there! =) This is alert");

// Prompt 

prompt("This is - Prompt - function");

let result = prompt("Do you use Linux?","No");

alert("Prompt accepts 2 values, watch code.")

let distro = prompt("What linux distro do you use?", "Debian");

alert(`You use ${distro}`);

/*
    Prompt function:

    prompt(value, default value );

*/

// Confirm

let study = confirm("Are you a student?");

alert(`Confim: ${study}`);
